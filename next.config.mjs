/** @type {import('next').NextConfig} */

// only for docker development
// const nextConfig = {
//   webpack: (config, _) => ({
//     ...config,
//     watchOptions: {
//       ...config.watchOptions,
//       poll: 800,
//       aggregateTimeout: 300,
//     },
//   }),
// };

const nextConfig = {};

export default nextConfig;
