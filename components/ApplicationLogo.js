const ApplicationLogo = (props) => (
  <span
    className={`font-lato self-center text-3xl font-extrabold whitespace-nowrap theme-text-shadow tracking-tighter ${props.className}`}
  >
    AM
  </span>
);

export default ApplicationLogo;
